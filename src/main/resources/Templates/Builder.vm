package $packageName;

public class $builderClassName {

private $className object = new $className();

public $className build(){
return object;
}

#foreach($method in $methods.keySet())

  #set($withMethod = $method.replace("set","with"))

public $builderClassName $withMethod($methods[$method] value){
object.$method(value);
return this;
}
#end
}
